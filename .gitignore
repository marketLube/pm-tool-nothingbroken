# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (contains sensitive API keys)
# Note: .env file exists and contains Supabase credentials
# Use .env.example as template for local development
.env
# Keep .env.local for local overrides
.env.local
# Production environment files
.env.production

# Assistant can access this file for database operations
# This file should contain the same credentials as .env but is accessible to AI assistant
# IMPORTANT: Only add this file if you want AI assistant to have database access
.env.assistant
.vercel
